<%= form_for @post do |f| %>
  <%= f.label :title %> <br>
  <%= f.text_field :title %><br>
  <%= f.label :text %> <br>
  <%= f.text_area :text %><br> 
  <%= collection_check_boxes(:post, :tag_ids, @tags, :id, :name) %> 
  
  <%= f.submit %>
<% end %>

